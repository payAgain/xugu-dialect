<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.xugu</groupId>
    <artifactId>xugu-dialect-parent</artifactId>
    <version>7.4.5.Final</version>
  </parent>

  <artifactId>demo-spring-boot</artifactId>
  <packaging>jar</packaging>

  <name>demo-spring-boot</name>
  <description>Spring Boot demo consuming xugu-dialect (scaffold entry only)</description>

  <properties>
    <!-- Force Hibernate aligned with ADR-0001; Boot 4.1.0 may default to an older 7.4.x -->
    <hibernate.version>7.4.5.Final</hibernate.version>
    <spring-boot.version>4.1.0</spring-boot.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring-boot.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>com.xugu</groupId>
      <artifactId>xugu-dialect</artifactId>
    </dependency>
    <dependency>
      <groupId>com.xugu</groupId>
      <artifactId>xugu-jdbc</artifactId>
      <version>12.3.6</version>
      <scope>system</scope>
      <systemPath>${maven.multiModuleProjectDirectory}/xugu-jdbc-12.3.6.jar</systemPath>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter</artifactId>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <version>${spring-boot.version}</version>
      </plugin>
    </plugins>
  </build>
</project>
