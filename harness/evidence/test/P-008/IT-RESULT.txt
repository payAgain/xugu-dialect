# P-008 IT Result (test RP-02 / test-p008-20260715)

**Gate:** `-Dxugu.run.integration=true`  
**URL:** `jdbc:xugu://127.0.0.1:5138/SYSTEM?...&compatiblemode=NONE`  
**Exit:** 0  
**Full suite:** `mvn -q test` exit 0 (offline); `mvn -q test -Dxugu.run.integration=true` exit 0

## Live metadata

```
P-008 live productName=[XuguDB] driverName=[XuguDB JDBC Driver] version=[XuguDB 12.0.0] major=12 minor=0
```

## Observed flows

| Flow | Test method | Result |
|---|---|---|
| explicit-dialect-config | `explicitDialect_sessionFactorySimpleQuery` | PASS (dialect=`XuguDialect`; insert + HQL select) |
| spi-dialect-resolver-autodetect | `spiAutoResolve_sessionFactoryWithoutExplicitDialect` | PASS (SPI → `XuguDialect` v12.0; count query) |

## XuguDialectResolverIT (all)

| Test | Result |
|---|---|
| `liveMetadata_productAndDriverContainXugu` | PASS |
| `explicitDialect_sessionFactorySimpleQuery` | PASS |
| `spiAutoResolve_sessionFactoryWithoutExplicitDialect` | PASS |

## Offline boundaries

| Suite | Result |
|---|---|
| `XuguDialectResolverTest` | PASS (match + MySQL/Oracle/Postgres/blank → null) |
| `XuguDialectServicesResourceTest` | PASS (classpath + classes + fresh jar) |

## Jar packaging spot-check

```
jar: xugu-dialect-7.4.5.Final.jar
entry: FOUND META-INF/services/org.hibernate.engine.jdbc.dialect.spi.DialectResolver
body: com.xugu.dialect.XuguDialectResolver
```

## Isolation (no READ UNCOMMITTED claim)

- Live Hibernate log: `Isolation level: READ_COMMITTED`
- `XuguDialect` javadoc explicitly: READ UNCOMMITTED is not a XuGu ISO_LEVEL value — not claimed
- Matrix A-XCUT-006: 文档不允许

## Cleanup / leftover probe

```
SELECT COUNT(*) FROM ALL_TABLES WHERE TABLE_NAME LIKE 'HIB_P008%' => 0
SELECT COUNT(*) FROM USER_TABLES WHERE TABLE_NAME LIKE 'HIB_P008%' => 0
```
