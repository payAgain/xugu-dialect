---
task_id: P-015
initiative_id: I-010
build_id: B-002
kind: phase
task_type: test
primary_owner: test
test_owner: test
risk_score: 4
conflict_score: 0
execution: serial
execution_mode: subagent-required
status: ready
dependencies: [P-011]
readiness_dimensions:
  - functional-correctness
  - maintainability
required_verification:
  commands: [build, test]
  observed_flows: [i010-sql-goldens, i010-join-fetch]
acceptance_doc: harness/evidence/test/I-010/P-015/ACCEPTANCE.md
verification_evidence: harness/evidence/test/I-010/P-015/verification.json
role_pipeline:
  - step_id: RP-01
    role: test
    purpose: Native SQL golden Unit files (LIMIT/lock order/IDENTITY DDL) + HQL join fetch one-to-many IT
    required: true
    status: pending
  - step_id: RP-02
    role: reviewer
    purpose: Readonly — goldens match dialect emit; join fetch smoke gated
    required: true
    status: pending
---

# P-015 中 — SQL 金标 + join fetch 烟测

## Goal
1. Unit 金标文件：LIMIT/OFFSET、FOR UPDATE+LIMIT 序、IDENTITY DDL 片段（参考 NativeSqlBaselineTests；可用 classpath resources）
2. Integration：HQL `join fetch` 一对多加载烟测

## Allowed paths
- `dialect/src/test/java/com/xugu/dialect/**`
- `dialect/src/test/resources/**`（golden SQL）
- `harness/evidence/test/I-010/P-015/**`
- `contracts/xuguefcore-parity-suite.md`（本行）

## Deliverables
- `XuguNativeSqlBaselineTest` + `dialect/src/test/resources/sql-baselines/**`
- `XuguHqlJoinFetchIT` + parent/child entities

## Notes
- 可抽取/固化既有 `XuguPaginationLockTest` / identity DDL 断言为 baseline 文件，避免漂移
- 勿引入 SKIP LOCKED / FOR SHARE 金标为正例

## Acceptance
- [ ] Unit goldens offline green
- [ ] Join fetch IT gated; must-commit
