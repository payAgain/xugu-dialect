---
task_id: P-002
initiative_id: I-006
build_id: null
kind: phase
task_type: code
primary_owner: implementer
code_owner: implementer
test_owner: test
risk_score: 6
conflict_score: 1
execution: serial
execution_mode: subagent-required
direct_exception_reason: null
status: blocked
blocker:
  id: BLOCK-I006-P002
  kind: dependency
  reason: Depends on P-001 consumer-path SSOT (Layer A gaps)
  owner: orchestrator
  waiting_for: P-001 accepted
  revisit_when: P-001 status=accepted
  next_action: Dispatch implementer → test → reviewer for Layer A
  created_at: "2026-07-18T15:13:00+08:00"
dependencies: [P-001]
readiness_dimensions:
  - functional-correctness
  - reliability
  - compatibility
  - maintainability
  - deployment-and-configuration
required_verification:
  commands:
    - build
    - test
  observed_flows:
    - layer-a-golden-path-boot-validate-crud-startup-spi
acceptance_doc: harness/evidence/implementer/I-006/P-002/ACCEPTANCE.md
verification_evidence: harness/evidence/test/I-006/P-002/verification.json
role_pipeline:
  - step_id: RP-01
    role: implementer
    purpose: close Layer A SSOT gaps — validate startup, startup-crud live path, full CRUD, JPQL+Pageable, optional SPI without explicit dialect
    required: true
    condition: null
    status: pending
    invocation_id: null
  - step_id: RP-02
    role: test
    purpose: offline mvn test green; when XUGU_RUN_IT=true exercise Layer A live paths; update SSOT A rows gap→covered
    required: true
    condition: null
    status: pending
    invocation_id: null
  - step_id: RP-03
    role: reviewer
    purpose: confirm Boot-level evidence (not dialect-unit-only); Layer A acceptance vs brief
    required: true
    condition: null
    status: pending
    invocation_id: null
evidence_writers:
  implementer: harness/evidence/implementer/I-006/P-002/**
  test: harness/evidence/test/I-006/P-002/**
  reviewer: harness/evidence/reviewer/I-006/P-002/**
handoff_writers:
  implementer:
    mode: file
    path: harness/handoffs/implementer/I-006-P-002.yaml
    file_writer: implementer
  test:
    mode: file
    path: harness/handoffs/test/I-006-P-002.yaml
    file_writer: test
---

# P-002 加固黄金路径 A（validate/CRUD/startup/SPI）

## Goal
关闭 P-001 SSOT 中 **Layer A** 缺口：Spring Boot 黄金路径——`ddl-auto=validate` 启动、startup-crud 真路径、完整 CRUD、JPQL + Pageable、可选 SPI 无显式 dialect 配置。

## Constraints
- GAV `com.xugu:xugu-dialect:7.4.5.Final`（不升版）
- 原生 `XuguDialect`；禁止继承 MySQL/Oracle；禁止 sibling 移植
- 方言模块原则上不改（除非发现 Boot 独有缺陷）
- 日常 `mvn test` 离线绿；Accept 级真库需 `XUGU_RUN_IT=true`

## Allowed paths
- `demo-spring-boot/**`（实体/配置/测试）
- `contracts/consumer-path-baseline.md`（状态更新）
- `harness/evidence/**/I-006/P-002/**`、`harness/handoffs/{implementer,test}/**`

## Forbidden paths
- Sibling / MySQLDialect / OracleDialect 移植
- Ship / tag / push / Central
- 完整 94 行镜像实现（非 A 范围）
- Layer B/C′ 新模型（留给 P-003/P-004）

## Out of scope
- Layer B 关联/SEQUENCE/锁/异常/回滚（P-003）
- Layer C′ 剩余扫盲（P-004）
- 用户文档最终对齐（P-005）

## Acceptance criteria
- [ ] Given P-001 Layer A gaps, when implementer adds Boot tests/config, then each A gap row is `covered` with `entry_class#method`; boundary: offline suite stays green without live DB; evidence: SSOT + TEST-REPORT
- [ ] Given validate + CRUD + JPQL Pageable + optional SPI paths, when run under agreed gates, then observable success/failure matches brief Layer A; failure: SPI-only unit test as sole evidence → reject; evidence: verification.json + observed_flows
- [ ] Given `mvn -q -DskipTests package` and `mvn -q test`, when Phase closes, then both pass; evidence: verification.json

## Impact analysis
- Interfaces / callers: demo Boot test surface expands; public dialect API unchanged unless Boot-only defect fix
- Data / migration: test schema via existing demo DDL strategy only
- Security / privacy: env credentials via existing demo support; no secrets committed
- Reliability / failure recovery: live DB connectivity under IT gate
- Performance / capacity: unaffected (no hot-path dialect work)
- Deployment / configuration / rollback: may document explicit vs SPI dialect config in tests
- Explicitly unaffected: Central publish, GAV bump, full C′ rows

## Readiness impact
| Dimension | Result |
|---|---|
| Functional correctness | required |
| Reliability | conditional — live DB under XUGU_RUN_IT |
| Compatibility | required — SPI + explicit dialect entry |
| Maintainability | required — SSOT updated |
| Deployment/config | conditional — Boot property / dialect resolution tests |
| Data integrity | conditional — CRUD mutates test data |
| Security | conditional — env credentials only |
| Performance | not-applicable |
| Observability | not-applicable |
| Rollback | not-applicable — no Ship |

## Validation
```text
mvn -q -DskipTests package
mvn -q test
# Accept-prep (when DB available): XUGU_RUN_IT=true mvn -q test
python harness/scripts/verify.py
```

## Observed affected flows
- Flow: layer-a-golden-path-boot-validate-crud-startup-spi
  - Method / environment: demo-spring-boot tests; offline default; gated live optional
  - Expected observation: validate startup, CRUD, JPQL+Pageable, SPI-without-explicit-dialect covered
  - Evidence path: harness/evidence/test/I-006/P-002/

## Notes for dispatcher
- 依赖 P-001；串行；不得跳过 reviewer
