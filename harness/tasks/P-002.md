---
task_id: P-002
initiative_id: I-010
build_id: null
kind: phase
task_type: implementation
primary_owner: implementer
code_owner: implementer
test_owner: test
risk_score: 6
conflict_score: 0
execution: serial
execution_mode: subagent-required
status: planned
blocker: null
dependencies: [P-001]
readiness_dimensions:
  - functional-correctness
  - compatibility
required_verification:
  commands: [build, test]
  observed_flows: [interval-entity-orm-roundtrip]
acceptance_doc: harness/evidence/implementer/I-010/P-002/ACCEPTANCE.md
role_pipeline:
  - step_id: RP-01
    role: implementer
    purpose: Entity Duration/INTERVAL_SECOND mapping + dialect hooks if needed for ORM surface
    required: true
    status: pending
  - step_id: RP-02
    role: test
    purpose: Gated live IT entity persist/load; or honest negative IT if JDBC/Hibernate surface unavailable
    required: true
    status: pending
  - step_id: RP-03
    role: reviewer
    purpose: Readonly — positive ORM live or documented negative; SSOT status update honest
    required: true
    status: pending
---

# P-002 A-TYP-014 INTERVAL 实体 ORM 真库往返

## Goal
将 A-TYP-014 从 native-only / known-limit 提升为 **实体 ORM 真库 round-trip**（Duration / INTERVAL_SECOND），或书面证明 JDBC/Hibernate 不可用并保留负向 IT。

## Deliverables
- Entity mapping path（参考 JSON/ARRAY 模式）
- Live IT under `XUGU_RUN_IT` **或** negative IT + SSOT/user-doc reason
- SSOT row update if promoted

## Out of scope
- 13 interval subtypes tooling-only strings (keep); Ship; invent SQL

## Acceptance
- [ ] Given entity with Duration/INTERVAL_SECOND, when persist+load on live DB, then values round-trip **OR** negative IT documents why ORM impossible
- [ ] verify/build/test evidence; reviewer ACCEPT
