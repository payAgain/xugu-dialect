---
task_id: P-004
initiative_id: I-001
build_id: null
kind: phase
task_type: code
primary_owner: implementer
code_owner: implementer
test_owner: test
risk_score: 8
conflict_score: 0
execution: serial
execution_mode: subagent-required
direct_exception_reason: null
status: ready
blocker: null
dependencies:
  - P-003
readiness_dimensions:
  - functional-correctness
  - performance-and-capacity
  - maintainability
  - compatibility
required_verification:
  commands:
    - build
    - test
  observed_flows:
    - limit-offset-pagination-real-db
    - pessimistic-lock-sql-real-db
acceptance_doc: harness/evidence/implementer/P-004/ACCEPTANCE.md
verification_evidence: harness/evidence/implementer/P-004/verification.json
role_pipeline:
  - step_id: RP-01
    role: implementer
    purpose: implement LimitHandler / lock SQL per matrix
    required: true
    condition: null
    status: pending
    invocation_id: null
  - step_id: RP-02
    role: test
    purpose: real DB IT for pagination and locks
    required: true
    condition: null
    status: pending
    invocation_id: null
  - step_id: RP-03
    role: reviewer
    purpose: review SQL generation for pagination/lock
    required: true
    condition: full_or_risk_ge_8
    status: pending
    invocation_id: null
evidence_writers:
  implementer: harness/evidence/implementer/P-004/**
  test: harness/evidence/test/P-004/**
handoff_writers:
  implementer:
    mode: file
    path: harness/handoffs/implementer/P-004.yaml
    file_writer: implementer
  test:
    mode: file
    path: harness/handoffs/test/P-004.yaml
    file_writer: test
---

# P-004 分页与锁语法

## Goal
实现定义 A 中 **分页（Limit/offset 等）与悲观锁**「可实现」项，并在真实 XuguDB 上验证。

## Allowed paths
- `dialect/src/**`（LimitHandler、LockingStrategy 等相关）
- 矩阵/契约勾选更新
- `harness/evidence/**/P-004/**`
- `harness/handoffs/**/P-004.*`

## Forbidden paths
- 旁路方言移植；继承 MySQL/Oracle Dialect；官方 content 写入；demo 大改

## Impact analysis
- Interfaces / callers: Hibernate 查询分页与 lock mode SQL
- Data / migration: 无 schema 结构变更（除非测试建表）
- Security / privacy: 无新密钥面
- Reliability / failure recovery: 锁超时/不支持组合须可观测
- Performance / capacity: 大 offset 行为需有矩阵/测试说明
- Deployment / configuration / rollback: 方言行为变更
- Explicitly unaffected surfaces: 序列生成器、函数注册、SPI

## Acceptance criteria
- [ ] Given 分页矩阵行，when 执行分页查询 IT，then SQL 与结果集符合文档且通过；boundary：不支持的分页模式标注延后；evidence: IT
- [ ] Given 锁矩阵行，when 使用悲观锁，then 生成/执行的锁 SQL 符合文档；boundary：不支持锁类型失败可诊断；evidence: IT
- [ ] Given 性能维度，when 热路径变更，then 有简要说明或微测/分析笔记；evidence: ACCEPTANCE

## Validation
```text
python harness/scripts/verify.py
```

## Observed affected flows
- Flow: limit-offset-pagination-real-db
  - Method / environment: 真实 XuguDB
  - Expected observation: 分页结果正确
  - Evidence path: harness/evidence/test/P-004/
- Flow: pessimistic-lock-sql-real-db
  - Method / environment: 真实 XuguDB
  - Expected observation: 锁 SQL 可执行或按矩阵正确拒绝
  - Evidence path: harness/evidence/test/P-004/

## Acceptance
- [ ] every pipeline condition was evaluated before dispatch or close
- [ ] required steps whose condition is true are `passed` with an `invocation_id`
- [ ] condition-false or optional unused steps are `skipped` with a recorded reason
- [ ] Test and Reviewer invocations use an independent context from implementation
- [ ] `VERIFY PASS` evidence covers every `required_verification.commands` entry
- [ ] every required observed flow was actually exercised and recorded
- [ ] all affected `readiness_dimensions` have evidence or a justified not-applicable decision
- [ ] acceptance criteria are observable and satisfied
- [ ] acceptance_doc 已写
- [ ] 有变更则 must-commit（记录 SHA）

## Notes for dispatcher
- 串行依赖 P-003；真实库必需。
