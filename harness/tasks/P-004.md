---
task_id: P-004
initiative_id: I-006
build_id: B-001
kind: phase
task_type: code
primary_owner: implementer
code_owner: implementer
test_owner: test
risk_score: 6
conflict_score: 2
execution: serial
execution_mode: subagent-required
direct_exception_reason: null
status: accepted
blocker: null
dependencies: [P-003]
readiness_dimensions:
  - functional-correctness
  - compatibility
  - maintainability
  - data-integrity
required_verification:
  commands:
    - build
    - test
  observed_flows:
    - layer-c-prime-remaining-boot-required-entries
acceptance_doc: harness/evidence/implementer/I-006/P-004/ACCEPTANCE.md
verification_evidence: harness/evidence/test/I-006/P-004/verification.json
role_pipeline:
  - step_id: RP-01
    role: implementer
    purpose: close remaining C′ Boot-required SSOT gaps (function subset, optional JSON, one bulk update/delete, extra types) — not full 94 mirror
    required: true
    condition: null
    status: passed
    invocation_id: inv-i006-p004-rp01-implementer
  - step_id: RP-02
    role: test
    purpose: offline green; gated live for new C′ entries; SSOT 0 open Boot-required gaps (dialect-it-only allowed)
    required: true
    condition: null
    status: passed
    invocation_id: inv-i006-p004-rp02-test
  - step_id: RP-03
    role: reviewer
    purpose: reject scope creep to full 94 Boot mirror; confirm demo test count ~25–40 @Test
    required: true
    condition: null
    status: passed
    invocation_id: inv-i006-p004-rp03-reviewer
evidence_writers:
  implementer: harness/evidence/implementer/I-006/P-004/**
  test: harness/evidence/test/I-006/P-004/**
  reviewer: harness/evidence/reviewer/I-006/P-004/**
handoff_writers:
  implementer:
    mode: file
    path: harness/handoffs/implementer/I-006-P-004.yaml
    file_writer: implementer
  test:
    mode: file
    path: harness/handoffs/test/I-006-P-004.yaml
    file_writer: test
---

# P-004 扫盲 C′ 剩余 Boot 必测入口

## Goal
关闭 P-001 SSOT 中 **Layer C′** 剩余 Boot 必测缺口：函数子集、JSON（可选）、bulk update/delete 一条、额外类型字段等——使消费者路径 SSOT **0 open gap**（`dialect-it-only` 除外）。Demo `@Test` 总量约 **25–40**。

## Constraints
- **C′ ≠ 94 行全量镜像**；仅 P-001 筛出的 Boot 必测行
- GAV `7.4.5.Final`；原生方言；不移植 sibling
- 纯方言钩子保持方言 IT，不强制 Boot 用例

## Allowed paths
- `demo-spring-boot/**`
- `contracts/consumer-path-baseline.md`
- `harness/evidence/**/I-006/P-004/**`、`harness/handoffs/{implementer,test}/**`
- `dialect/**` only for proven Boot-only defects

## Forbidden paths
- Implementing full Definition A + ruler C as Boot tests
- Ship / 升版
- Rewriting I-005 dialect negative baseline (unless SSOT xref requires note)

## Out of scope
- Final docs / VERIFY Accept prep（P-005）
- Layer A/B already closed in P-002/P-003

## Acceptance criteria
- [ ] Given C′ gap rows in SSOT, when implementer closes them, then each Boot-required row is `covered` or justified `dialect-it-only`/`known-limit-documented`; boundary: no inventing SQL outside docs; evidence: SSOT + TEST-REPORT
- [ ] Given demo test suite, when counted, then ~25–40 `@Test` (order-of-magnitude); failure: scope creep to 94 Boot mirror → reject; evidence: reviewer REVIEW
- [ ] Given build+test, when Phase closes, then offline PASS; evidence: verification.json

## Impact analysis
- Interfaces / callers: additional Boot consumer scenarios
- Data / migration: possible extra columns/types in demo entities
- Security / privacy: unchanged
- Reliability / failure recovery: bulk update/delete failure shapes if exercised
- Performance / capacity: not primary goal
- Deployment / configuration / rollback: Boot config only as needed for types/JSON
- Explicitly unaffected: Ship, GAV, dialect-only SPI rows

## Readiness impact
| Dimension | Result |
|---|---|
| Functional correctness | required |
| Compatibility | required — type/function consumer entry |
| Maintainability | required — SSOT 0 Boot-required gap |
| Data integrity | conditional — bulk DML / type mapping |
| Reliability | conditional — if bulk/JSON failure paths added |
| Deployment/config | conditional — Boot props for types/JSON |
| Security | conditional — env credentials |
| Performance | not-applicable |
| Observability | not-applicable |
| Rollback | not-applicable |

## Validation
```text
mvn -q -DskipTests package
mvn -q test
# Prefer when DB available: XUGU_RUN_IT=true mvn -q test
```

## Observed affected flows
- Flow: layer-c-prime-remaining-boot-required-entries
  - Method / environment: demo-spring-boot tests covering remaining C′ SSOT rows
  - Expected observation: Boot-required SSOT has zero open gaps; dialect-it-only rows documented
  - Evidence path: harness/evidence/test/I-006/P-004/

## Notes for dispatcher
- 依赖 P-003；串行；reviewer 重点防范围膨胀
