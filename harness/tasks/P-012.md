---
task_id: P-012
initiative_id: I-010
build_id: B-002
kind: phase
task_type: test
primary_owner: test
code_owner: null
test_owner: test
risk_score: 5
conflict_score: 0
execution: serial
execution_mode: subagent-required
status: ready
blocker: null
dependencies: [P-011]
readiness_dimensions:
  - functional-correctness
  - reliability
required_verification:
  commands: [build, test]
  observed_flows: [i010-optimistic-lock, i010-hql-groupby-count]
acceptance_doc: harness/evidence/test/I-010/P-012/ACCEPTANCE.md
verification_evidence: harness/evidence/test/I-010/P-012/verification.json
role_pipeline:
  - step_id: RP-01
    role: test
    purpose: Implement gated ITs XuguOptimisticConcurrencyIT + XuguHqlGroupByCountIT (+ entities); offline compile green; live SKIPPED_INFRA ok
    required: true
    status: pending
  - step_id: RP-02
    role: reviewer
    purpose: Readonly — stale @Version fails correctly; GroupBy/Count materialize; no EF copy; gate documented
    required: true
    condition: null
    status: pending
---

# P-012 高优先级 — 乐观锁 + HQL GroupBy/Count

## Goal
落地两项高优先级 Integration 用例（门控 `XUGU_RUN_IT` / `xugu.run.integration`）。

## Allowed paths
- `dialect/src/test/java/com/xugu/dialect/**`
- `harness/evidence/test/I-010/P-012/**`
- `harness/handoffs/test/**`
- SSOT status updates in `contracts/xuguefcore-parity-suite.md`（仅本 Phase 行）

## Deliverables
1. `XuguOptimisticConcurrencyIT` — entity `@Version`; concurrent stale write → `OptimisticLockException` (or Hibernate equivalent); ref EF OptimisticConcurrencyTests
2. `XuguHqlGroupByCountIT` — HQL `count` scalar + `group by` count projection materialize Int; ref RuntimeGapBaselineTests
3. Entities under `dialect/.../it/entities/`
4. Offline `mvn -q -DskipTests package` + `mvn -q test` PASS（gated ITs assume-skip when gate off）

## Acceptance
- [ ] Gate off → tests skipped/assumptions, suite green
- [ ] Gate on + DB → assert stale lock fail + groupby/count; else SKIPPED_INFRA documented honestly
- [ ] VERIFY commands build+test; must-commit SHA
