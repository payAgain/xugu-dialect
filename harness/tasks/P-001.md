---
task_id: P-001
initiative_id: I-002
build_id: B-001
kind: phase
task_type: code
primary_owner: implementer
code_owner: implementer
test_owner: test
risk_score: 8
conflict_score: 0
execution: serial
execution_mode: subagent-required
direct_exception_reason: null
status: accepted
blocker: null
dependencies: []
readiness_dimensions:
  - functional-correctness
  - performance-and-capacity
  - maintainability
  - compatibility
required_verification:
  commands:
    - build
    - test
  observed_flows:
    - hql-pagination-offset-fetch-real-db
acceptance_doc: harness/evidence/implementer/P-001/ACCEPTANCE.md
verification_evidence: harness/evidence/test/P-001/verification.json
role_pipeline:
  - step_id: RP-01
    role: implementer
    purpose: implement XuguSqlAstTranslator + factory wiring for HQL offset/fetch → Xugu LIMIT
    required: true
    condition: null
    status: passed
    invocation_id: impl-p001-20260715
    note: "initial impl; MAJOR lock+page evidence under impl-p001-fix-locklimit-20260715"
  - step_id: RP-01b
    role: implementer
    purpose: fix MAJOR — gated IT proving AST/HQL FOR UPDATE before LIMIT (+ WAIT after)
    required: true
    condition: null
    status: passed
    invocation_id: impl-p001-fix-locklimit-20260715
    note: "XuguHqlPaginationIT.hqlLockAndPageEmitsForUpdateBeforeLimitAndWaitAfter; live SQL captured"
  - step_id: RP-02
    role: test
    purpose: add/run gated real-DB IT for HQL setFirstResult/setMaxResults; VERIFY PASS
    required: true
    condition: null
    status: passed
    invocation_id: test-p001-retest-20260715
    note: "retest after lock+page fix; prior test-p001-20260715 superseded for Accept evidence"
  - step_id: RP-03
    role: reviewer
    purpose: review SqlAstTranslator SQL generation vs LimitHandler / FOR UPDATE order
    required: true
    condition: full_or_risk_ge_8
    status: passed
    invocation_id: rev-p001-recheck-20260715
    decision: approve
    note: "MAJOR CLOSED; supersedes request-changes rev-p001-20260715"
evidence_writers:
  implementer: harness/evidence/implementer/P-001/**
  test: harness/evidence/test/P-001/**
handoff_writers:
  implementer:
    mode: file
    path: harness/handoffs/implementer/P-001.yaml
    file_writer: implementer
  test:
    mode: file
    path: harness/handoffs/test/P-001.yaml
    file_writer: test
---

# P-001 修复 HQL 分页 SqlAstTranslator

## Goal
消除 HQL/Criteria `setFirstResult`/`setMaxResults` 生成 ANSI `OFFSET`/`FETCH` 导致虚谷 `[E19132] unexpected OFFSET` 的 P0；本仓补充门控真实库 IT 并 VERIFY PASS。版本保持 **7.4.5.Final**。

## Allowed paths
- `dialect/src/main/java/**`（`XuguSqlAstTranslator`、`getSqlAstTranslatorFactory()`、与 LimitHandler 一致的 offset/fetch 渲染）
- `dialect/src/test/java/**`（HQL 分页 IT / 相关单测）
- `contracts/feature-matrix-definition-a.md`、`contracts/xugu-dialect.contract.md`（仅本 Phase 相关勾选/勘误）
- `harness/evidence/**/P-001/**`
- `harness/handoffs/**/P-001.*`

## Forbidden paths
- `getQuerySequencesString` / sequence metadata（属 P-002）
- 升版本号；Ship；旁路方言移植（含 `E:\Work\java\hibernate-dialect`）
- `demo-spring-boot/**` 大改；官方 `E:\Work\docs\content` 写入
- P-002 / P-003 文档矩阵大扫尾（留给后续 Phase）

## Impact analysis
- Interfaces / callers: Hibernate SQL AST → 方言 SQL；HQL/Criteria 分页调用方
- Data / migration: 无持久 schema 变更（IT 可临时建表）
- Security / privacy: 无新密钥面
- Reliability / failure recovery: 非法/不支持分页组合须可诊断（与既有 LimitHandler 行为一致）
- Performance / capacity: offset/fetch 热路径；须与 `XuguLimitHandler` 语义一致
- Deployment / configuration / rollback: 同 GAV `7.4.5.Final` 行为修复；无配置键变更预期
- Explicitly unaffected surfaces: sequence metadata validate、demo 业务、用户指南全文

## Acceptance criteria
- [x] Given SessionFactory 绑定 `XuguDialect` 且真实库可达，when HQL/Criteria 使用 `setFirstResult`/`setMaxResults`，then 生成/执行的 SQL 使用虚谷 LIMIT（非裸 ANSI OFFSET/FETCH），查询成功且结果窗口正确；boundary：与 FOR UPDATE 组合时顺序与 LimitHandler 一致；evidence: 门控 IT + SQL/日志摘录
- [x] Given 离线环境，when `mvn -q test`（无 `-Dxugu.run.integration=true`），then 仍 PASS；evidence: verify/test 证据
- [x] Given 本 Phase 变更，when `python harness/scripts/verify.py`，then **VERIFY PASS**；evidence: `harness/evidence/test/P-001/verification-retest.json`
- [x] Given 制品坐标，when 检查 POM/产物版本，then 仍为 **7.4.5.Final**；evidence: ACCEPTANCE 记录

## Validation
```text
mvn -q -DskipTests package
mvn -q test
mvn -q test -Dxugu.run.integration=true
python harness/scripts/verify.py
python harness/scripts/branch_check.py
```

## Observed affected flows
- Flow: hql-pagination-offset-fetch-real-db
  - Method / environment: 真实 XuguDB；门控 IT（`-Dxugu.run.integration=true`）
  - Expected observation: HQL 分页不再抛 E19132；结果集窗口正确
  - Evidence path: harness/evidence/test/P-001/
- Flow: hql-lock-page-for-update-order-real-db
  - Method: `XuguHqlPaginationIT.hqlLockAndPageEmitsForUpdateBeforeLimitAndWaitAfter`
  - Expected observation: FOR UPDATE before LIMIT (+ WAIT after)
  - Evidence path: harness/evidence/test/P-001/IT-RESULT-RETEST.txt

## Acceptance
- [x] every pipeline condition was evaluated before dispatch or close
- [x] required steps whose condition is true are `passed` with an `invocation_id`
- [x] condition-false or optional unused steps are `skipped` with a recorded reason
- [x] Test and Reviewer invocations use an independent context from implementation
- [x] `VERIFY PASS` evidence covers every `required_verification.commands` entry
- [x] every required observed flow was actually exercised and recorded
- [x] all affected `readiness_dimensions` have evidence or a justified not-applicable decision
- [x] acceptance criteria are observable and satisfied
- [x] acceptance_doc 已写
- [x] 有变更则 must-commit（记录 SHA）

## Notes for dispatcher
- 进度单位 = 本 Phase；执行 = `role_pipeline`（implementer → test → reviewer）。
- 默认串行；禁止询问人类并行。
- 证据来源：`E:\Work\java\xugu-hibernate-test\TEST-REPORT.md`（P0）；本仓 IT 为验收主证据。
- 禁止阅读/复制旁路 `hibernate-dialect` 仓实现。
